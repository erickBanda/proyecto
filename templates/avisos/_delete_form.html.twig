<form method="post" action="{{ path('app_avisos_delete', {'id': aviso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aviso.id) }}">
    <button class="btn">Delete</button>
</form>
